services:
  - type: web
    name: video-downloader
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: bash -c "apt-get update -qq && apt-get install -y -qq ffmpeg python3 python3-pip && pip3 install -q yt-dlp && node server.js"
    envVars:
      - key: PORT
        value: 10000
      - key: YTDLP_PATH
        value: yt-dlp
      - key: FFMPEG_PATH
        value: ffmpeg
      - key: NODE_ENV
        value: production
